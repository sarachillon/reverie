import 'package:flutter/material.dart';

class ArmarioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mi Armario')),
      body: Center(child: Text('Contenido del armario')),
    );
  }
}
