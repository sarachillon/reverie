import 'package:flutter/material.dart';

class BuscadorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Buscador')),
      body: Center(child: Text('Aquí irá el buscador')),
    );
  }
}
